# --- Core numerics / stats
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1

# --- HTTP clients
requests==2.32.3
aiohttp==3.9.5

# --- Understat API wrapper (PyPI version that works on Python 3.11)
understat==0.1.12

# --- Time handling (used by pandas)
python-dateutil==2.9.0.post0
pytz==2024.1

# --- Utilities (optional but handy for progress bars)
tqdm==4.66.4
